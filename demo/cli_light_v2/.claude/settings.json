{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "agent",
            "prompt": "Decide if workflow is complete. Read .claude/state/workflow.json and determine if all steps are done. If not done, return {\"ok\": false, \"reason\": \"Workflow still running\"}. Otherwise return {\"ok\": true}."
          }
        ]
      }
    ]
  }
}
