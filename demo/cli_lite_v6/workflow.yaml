start:
  task: Analyze error {{state.error}}
  next: fix

fix:
  task: Propose a fix for {{state.error}}
  next: test

test:
  task: Validate the fix
  transitions:
    - if: success
      goto: done
    - if: fail
      goto: fix

done:
  task: Summarize the final solution
